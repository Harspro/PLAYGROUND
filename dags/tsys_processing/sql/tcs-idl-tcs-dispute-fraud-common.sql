MERGE pcb-{env}-landing.domain_dispute.TCS_DISPUTE_FRAUD_COMMON T
USING pcb-{env}-processing.domain_dispute.TCS_DISPUTE_FRAUD_COMMON_IDL S
      ON  T.FILE_HDR_DT = (CAST(S.REC_CREATE_TMS AS DATE)-1)
     AND T.PZINSKEY = S.PZINSKEY
     AND T.INTERACTIONID = S.INTERACTIONID
     AND T.INTCARDNUM = S.INTCARDNUM
     AND T.ACCOUNTNUMBER = S.ACCOUNTNUMBER
     AND T.CREATEOPERATOR = S.CREATEOPERATOR
     AND T.INTCUSTID = S.INTCUSTID
     AND T.WTCUSTID = S.WTCUSTID
     AND T.TSYSACCOUNTID = S.TSYSACCOUNTID
WHEN NOT MATCHED THEN INSERT (
    PZINSKEY,
    PXINDEXCOUNT,
    TYNOTESLIST_ID,
    TYACCOUNTLIST_ID,
    INTERACTIONTYPE,
    INTERACTIONID,
    INTENTTASKNAME,
    INTSTATUS,
    WTSTATUS,
    INTOPNAME,
    CLIENTCUSTOMERID,
    INTCARDNUM,
    INTCREATEDATETIME,
    INTCOMPLETIONDATETIME,
    ERRORSTATUS,
    URGENCYWORK,
    WORKTYPENAME,
    PARENTWORKTYPE,
    RESOLVEDUSERID,
    ACCOUNTNUMBER,
    WTCREATEDATETIME,
    WTUPDATEDATETIME,
    WTCOMPLETIONDATETIME,
    CREATEOPERATOR,
    WTUPDTOPID,
    SLADEADLINE,
    SLAGOAL,
    INTCUSTID,
    RESOLVEDUSERWORKGROUP,
    NOTES,
    NOTESLISTID,
    CURRENTINTNOTES,
    WTCUSTID,
    DURATION,
    TSYSACCOUNTID,
    CLIENTID,
    WTAPPLICATION,
    CTIVERIFICATION,
    PRODUCTNAME,
    CPC,
    TPC,
    PYLABEL,
    REC_CREATE_TMS,
    REC_CHNG_TMS,
    REC_CHNG_ID,
    BATCH_RUN_LOG_UID,
    FILE_HDR_DT)
VALUES(
    S.PZINSKEY,
    CAST(S.PXINDEXCOUNT AS INT64),
    CAST(S.TYNOTESLIST_ID AS INT64),
    CAST(S.TYACCOUNTLIST_ID AS INT64),
    S.INTERACTIONTYPE,
    S.INTERACTIONID,
    S.INTENTTASKNAME,
    S.INTSTATUS,
    S.WTSTATUS,
    S.INTOPNAME,
    S.CLIENTCUSTOMERID,
    S.INTCARDNUM,
    CAST(S.INTCREATEDATETIME AS DATETIME),
    CAST(S.INTCOMPLETIONDATETIME AS DATETIME),
    S.ERRORSTATUS,
    CAST(S.URGENCYWORK AS NUMERIC),
    S.WORKTYPENAME,
    S.PARENTWORKTYPE,
    S.RESOLVEDUSERID,
    S.ACCOUNTNUMBER,
    CAST(S.WTCREATEDATETIME AS DATETIME),
    CAST(S.WTUPDATEDATETIME AS DATETIME),
    CAST(S.WTCOMPLETIONDATETIME AS DATETIME),
    CREATEOPERATOR,
    WTUPDTOPID,
    SLADEADLINE,
    SLAGOAL,
    INTCUSTID,
    RESOLVEDUSERWORKGROUP,
    NOTES,
    NOTESLISTID,
    CURRENTINTNOTES,
    WTCUSTID,
    DURATION,
    TSYSACCOUNTID,
    CLIENTID,
    WTAPPLICATION,
    CTIVERIFICATION,
    PRODUCTNAME,
    CPC,
    TPC,
    PYLABEL,
    CAST(S.REC_CREATE_TMS AS DATETIME),
    CAST(S.REC_CHNG_TMS AS DATETIME),
    'IDL-' || S.REC_CHNG_ID,
    CAST(S.BATCH_RUN_LOG_UID as int64),
    DATE(S.REC_CREATE_TMS)-1)
 ;