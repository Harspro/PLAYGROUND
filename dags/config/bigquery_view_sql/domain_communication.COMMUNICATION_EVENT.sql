SELECT
    requestId,
    notificationCode,
    contactStrategyCode,
    language,
    source,
    sourceId,
    domainCode,
    customerId,
    pcfCustomerId,
    accountId,
    tsysCustomerId,
    tsysAccountId,
    productCode,
    lastDigits,
    timezone,
    statusChannel,
    statusCode,
    statusResponse,
    statusMessage,
    statusTimestamp,
    statusContact,
    statusExternalId,
    statusElapsedTime,
    message_id,
    subscription_name,
    attributes,
    publish_time,
    data
FROM pcb-{env}-landing.domain_communication.COMMUNICATION_EVENT
WHERE cast(publish_time as date) > '1900-01-01'