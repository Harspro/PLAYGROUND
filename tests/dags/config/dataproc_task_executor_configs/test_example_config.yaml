# Example test configuration
example_dataproc_serverless_cluster_job:
  default_args:
    owner: "team-defenders-alerts"
    capability: "risk-management"
    sub_capability: "TBC"
    severity: "TBC"
    business_impact: "TBC"
    customer_impact: "TBC"
    retries: 3
    retry_delay: 300
  dag:
    dagrun_timeout: 120
    start_date: "2025-01-01"
    catchup: false
    description: "TBC"
    tags: [ "team-defenders-alerts" , "TBC" ]
  spark_job:
    jar_file_uris:
      - gs://pcb-{env}-staging-artifacts/jarfiles/test-app.jar
    main_class: com.example.TestMain
    file_uris:
      - gs://pcb-{env}-staging-artifacts/resources/test-app/config.yaml
    args:
      app.mode: test
      app.flag: true
      app.config: config.yaml
    properties:
      spark.driver.extraJavaOptions: -Duser.timezone=America/Toronto
      spark.executor.extraJavaOptions: -Duser.timezone=America/Toronto
